* auth_router:
	* password is stored as plain text
	* In memory otp store.
* base.py - asyncclient never closed
* report generator is created but never used
* No type hints in any functions, error handling inconsistant
* finanical metrics - pagination (is required)?
* Don't use print for logging 
* save data
	* save watchlist data to db
	* save research dashboard data in cache until user log outs
